# The McCance source workbook is large (~4.6 MB) and only needed to rebuild
# foods.csv. The built foods.csv is committed; see data/source/SOURCE.md
# to fetch the workbook when refreshing the database.
skills/diet-planner/data/source/*.xlsx

__pycache__/
*.pyc
